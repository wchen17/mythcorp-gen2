'use client';

import { ComingSoon } from '../components/ComingSoon';

export default function ContactPage() {
  return (
    <ComingSoon
      title="Say hello"
      blurb="No real form here yet — but the inbox is open."
      detail="If you actually want to reach out, drop a line via any link in /will. The fake phone number that used to live here has been retired."
    />
  );
}
